create table students (
                          id bigint not null auto_increment,
                          comment varchar(255),
                          e_mail varchar(255),
                          git_hub varchar(255),
                          name varchar(255),
                          primary key (id)
)