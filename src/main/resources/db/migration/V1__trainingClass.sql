create table classes (
                         id bigint not null auto_increment,
                         end date,
                         name varchar(255),
                         start date,
                         primary key (id)
);